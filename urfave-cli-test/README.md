```bash
./urfave-cli-test
NAME:
   boom - make an explosive entrance

USAGE:
   main [global options] command [command options] [arguments...]

VERSION:
   0.0.0

COMMANDS:
     server   server test
     help, h  Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --help, -h     show help
   --version, -v  print the version
```

```bash
./urfave-cli-test
NAME:
   main server - server test

USAGE:
   main server [command options] [arguments...]

OPTIONS:
   --config value       config file
   --gh-username value  gh username [$GH_USERNAME]
```
